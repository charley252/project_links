# OS-specific
.DS_Store
Thumbs.db

# Node.js dependencies (if using build tools like webpack, vite, etc.)
node_modules/

# Build output (if using a static site generator or bundler)
dist/
build/

# Optional: log files or temporary files
*.log
*.tmp

# Environment variables (if any)
.env
